// import { Branch } from "../types";

// const processBranch = (branch: Branch, level: number = 0): Branch => {
//   return {
//     ...branch,
//     level,
//     children: branch.children?.map((child) => processBranch(child, level + 1)),
//   };
// };

// export const processBranches = (branches: Branch[]): Branch[] => {
//   return branches.map((branch) => processBranch(branch));
// };
